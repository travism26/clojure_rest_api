(ns clojure-rest.api.walmart.api-call
  (:require [clj-http.client :as client]))

; make some request to walmart api





;not needed keeping for reference.
;; (defn dummy-data [] 
;;   (client/get "http://jsonplaceholder.typicode.com/posts/" {:accept :json}))
